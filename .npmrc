# node_modules plano (estilo npm). Necessário para o autolinking nativo do
# React Native / Expo, que não lida bem com a árvore simlinkada padrão do pnpm
# — sem isso o EAS Build falha no prebuild/autolinking.
node-linker=hoisted
